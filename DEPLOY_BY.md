# Размещение проекта на белорусских платформах

## 🇧🇾 Белорусские хостинг-провайдеры

### 1. ActiveCloud (Рекомендуется) ⭐

**Почему ActiveCloud:**
- Белорусская облачная платформа
- Виртуальные машины (VPS) с полным контролем
- Поддержка файловой системы
- Удобная панель управления
- Выгодные цены

**Как развернуть:**

#### Вариант A: VPS с Docker (рекомендуется)

1. **Создайте аккаунт:**
   - Перейдите на https://activecloud.by (или сайт провайдера)
   - Зарегистрируйтесь и выберите тариф VPS

2. **Создайте Dockerfile:**
   ```dockerfile
   FROM node:18-alpine
   WORKDIR /app
   COPY package*.json ./
   RUN npm ci --only=production
   COPY . .
   RUN npm run build
   EXPOSE 3000
   CMD ["npm", "start"]
   ```

3. **Создайте .dockerignore:**
   ```
   node_modules
   .next
   .git
   .env.local
   ```

4. **На VPS сервере:**
   ```bash
   # Установите Docker (если не установлен)
   # Загрузите проект
   git clone https://github.com/ваш-username/ваш-репозиторий.git
   cd ваш-репозиторий
   
   # Соберите образ
   docker build -t virtual-gallery .
   
   # Запустите контейнер
   docker run -d -p 3000:3000 --name gallery virtual-gallery
   ```

5. **Настройте Nginx (опционально):**
   ```nginx
   server {
       listen 80;
       server_name ваш-домен.by;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

**Стоимость:** От ~15-30 BYN/месяц (зависит от конфигурации)

---

### 2. hoster.by

**Почему hoster.by:**
- Белорусский хостинг-провайдер
- Поддержка Node.js
- Простое управление

**Как развернуть:**

1. **Создайте аккаунт:**
   - Перейдите на https://hoster.by
   - Выберите тариф с поддержкой Node.js

2. **Загрузите проект:**
   - Через FTP/SFTP или Git
   - Установите зависимости: `npm install`
   - Соберите проект: `npm run build`

3. **Настройте запуск:**
   - Обычно через панель управления хостингом
   - Укажите команду запуска: `npm start`
   - Настройте порт (обычно 3000)

4. **Настройте домен:**
   - В панели управления привяжите домен
   - Настройте DNS записи

**Стоимость:** От ~10-20 BYN/месяц

---

### 3. А1 (Белтелеком)

**Почему А1:**
- Крупный белорусский провайдер
- Дата-центр с сертификатом Tier III
- Надежная инфраструктура

**Как развернуть:**

1. **Свяжитесь с А1:**
   - Обратитесь в отдел корпоративных услуг
   - Узнайте о тарифах на VPS/облачные сервисы

2. **Настройте сервер:**
   - Получите доступ к VPS
   - Установите Node.js и зависимости
   - Загрузите проект

3. **Деплой:**
   ```bash
   # На сервере
   git clone ваш-репозиторий
   cd проект
   npm install
   npm run build
   npm start
   ```

**Стоимость:** Уточняйте у провайдера

---

## 📋 Пошаговая инструкция для ActiveCloud VPS

### Шаг 1: Подготовка проекта

1. **Создайте Dockerfile** (если используете Docker):
   ```dockerfile
   FROM node:18-alpine
   WORKDIR /app
   COPY package*.json ./
   RUN npm ci
   COPY . .
   RUN npm run build
   EXPOSE 3000
   CMD ["npm", "start"]
   ```

2. **Создайте .dockerignore:**
   ```
   node_modules
   .next
   .git
   .env.local
   ```

3. **Проверьте сборку локально:**
   ```bash
   npm run build
   ```

### Шаг 2: Загрузка в GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### Шаг 3: Настройка VPS на ActiveCloud

1. **Подключитесь к VPS:**
   ```bash
   ssh root@ваш-ip-адрес
   ```

2. **Установите необходимые инструменты:**
   ```bash
   # Обновление системы (для Ubuntu/Debian)
   apt update && apt upgrade -y
   
   # Установка Node.js
   curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
   apt install -y nodejs
   
   # Установка Git
   apt install -y git
   
   # Установка Docker (опционально)
   curl -fsSL https://get.docker.com -o get-docker.sh
   sh get-docker.sh
   ```

3. **Загрузите проект:**
   ```bash
   git clone https://github.com/ваш-username/ваш-репозиторий.git
   cd ваш-репозиторий
   ```

4. **Установите зависимости и соберите:**
   ```bash
   npm install
   npm run build
   ```

5. **Запустите проект:**
   ```bash
   # Простой запуск (для теста)
   npm start
   
   # Или используйте PM2 для постоянной работы
   npm install -g pm2
   pm2 start npm --name "virtual-gallery" -- start
   pm2 save
   pm2 startup
   ```

### Шаг 4: Настройка Nginx (для домена)

1. **Установите Nginx:**
   ```bash
   apt install -y nginx
   ```

2. **Создайте конфигурацию:**
   ```bash
   nano /etc/nginx/sites-available/virtual-gallery
   ```

3. **Добавьте конфигурацию:**
   ```nginx
   server {
       listen 80;
       server_name ваш-домен.by www.ваш-домен.by;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

4. **Активируйте конфигурацию:**
   ```bash
   ln -s /etc/nginx/sites-available/virtual-gallery /etc/nginx/sites-enabled/
   nginx -t
   systemctl reload nginx
   ```

5. **Настройте SSL (Let's Encrypt):**
   ```bash
   apt install -y certbot python3-certbot-nginx
   certbot --nginx -d ваш-домен.by -d www.ваш-домен.by
   ```

---

## 🔧 Альтернатива: Использование PM2

PM2 - менеджер процессов для Node.js, который автоматически перезапускает приложение при сбоях.

### Установка и настройка PM2:

```bash
# Установка
npm install -g pm2

# Запуск проекта
cd /path/to/your/project
pm2 start npm --name "virtual-gallery" -- start

# Сохранение конфигурации
pm2 save

# Настройка автозапуска при перезагрузке сервера
pm2 startup
# Выполните команду, которую выведет PM2

# Полезные команды
pm2 list          # Список процессов
pm2 logs          # Логи
pm2 restart all   # Перезапуск
pm2 stop all      # Остановка
pm2 delete all    # Удаление
```

---

## 📝 Настройка файловой системы

Ваш проект использует файловую систему для хранения данных. Убедитесь, что:

1. **Папки существуют:**
   ```bash
   mkdir -p public/models
   mkdir -p public/images
   mkdir -p data
   ```

2. **Права доступа:**
   ```bash
   chmod -R 755 public
   chmod -R 755 data
   ```

3. **Файлы данных:**
   - Убедитесь, что `data/exhibits.json` и `data/pageContent.json` существуют
   - Если их нет, создайте пустые файлы:
   ```bash
   echo '[]' > data/exhibits.json
   echo '{"siteSettings":{},"homePage":{},"aboutPage":{}}' > data/pageContent.json
   ```

---

## 🔒 Безопасность

1. **Настройте файрвол:**
   ```bash
   # UFW (Ubuntu)
   ufw allow 22/tcp    # SSH
   ufw allow 80/tcp     # HTTP
   ufw allow 443/tcp    # HTTPS
   ufw enable
   ```

2. **Измените пароль root:**
   ```bash
   passwd
   ```

3. **Настройте SSH ключи** (вместо пароля)

4. **Регулярно обновляйте систему:**
   ```bash
   apt update && apt upgrade -y
   ```

---

## 💰 Стоимость

| Провайдер | Тариф | Стоимость | Примечание |
|-----------|-------|-----------|------------|
| **ActiveCloud** | VPS Basic | ~15-30 BYN/мес | Зависит от конфигурации |
| **hoster.by** | Node.js хостинг | ~10-20 BYN/мес | Базовый тариф |
| **А1** | VPS/Облако | Уточнять | Корпоративные тарифы |

---

## ✅ Чек-лист перед деплоем

- [ ] Проект собирается: `npm run build`
- [ ] Код загружен в GitHub
- [ ] Создан Dockerfile (если используете Docker)
- [ ] Настроены папки для файлов (`public/models`, `public/images`, `data`)
- [ ] Настроен PM2 или другой процесс-менеджер
- [ ] Настроен Nginx (если используете домен)
- [ ] Настроен SSL сертификат
- [ ] Настроен файрвол
- [ ] Настроено резервное копирование

---

## 🆘 Полезные команды

```bash
# Проверка статуса Node.js
node --version
npm --version

# Проверка портов
netstat -tulpn | grep 3000

# Логи приложения (PM2)
pm2 logs virtual-gallery

# Перезапуск после изменений
pm2 restart virtual-gallery

# Мониторинг ресурсов
htop
df -h  # Дисковое пространство
free -h  # Память
```

---

## 📞 Контакты провайдеров

- **ActiveCloud**: https://activecloud.by (или уточните актуальный сайт)
- **hoster.by**: https://hoster.by
- **А1**: https://www.a1.by (корпоративные услуги)

---

## 🎉 После деплоя

1. Откройте ваш сайт по IP или домену
2. Проверьте все страницы
3. Проверьте админ-панель
4. Добавьте экспонаты
5. Проверьте виртуальную галерею

**Готово! Ваш проект размещен на белорусской платформе! 🇧🇾**







